{
  "enable_group_whitelist": {
    "type": "bool",
    "description": "是否启用群聊白名单，关闭后插件在所有群生效。",
    "default": true
  },
  "group_whitelist": {
    "type": "list",
    "description": "允许启用插件功能的群号列表。",
    "items": {
      "type": "string"
    },
    "hint": "请输入 QQ 群号，使用字符串格式。",
    "default": []
  },
  "enable_blacklist_notice": {
    "type": "bool",
    "description": "退群入黑名单和拒绝申请时是否在群内广播提示。",
    "default": true
  },
  "enable_auto_approve": {
    "type": "bool",
    "description": "是否自动同意非黑名单成员的入群申请。",
    "default": false
  },
  "leave_notice_template": {
    "type": "string",
    "description": "退群提示模板，可使用 {member}、{user_id}、{group_id} 占位符。",
    "default": "成员 {member} 已退出群聊，并被加入黑名单。"
  },
  "reject_reason": {
    "type": "string",
    "description": "当拒绝黑名单成员加群时提交给协议端的理由。",
    "default": "黑名单成员，拒绝加入。"
  },
  "admin_ids": {
    "type": "list",
    "description": "插件管理员 QQ 号，未来可拓展手动指令管理黑名单。",
    "items": {
      "type": "string"
    },
    "default": []
  }
}
